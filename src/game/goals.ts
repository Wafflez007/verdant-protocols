export type Goal = {
  description: string;
  targetScore: number;
};

export const level1Goal: Goal = {
  description: "Restore enough biodiversity for the ecosystem to sustain itself.",
  targetScore: 3000,
};
